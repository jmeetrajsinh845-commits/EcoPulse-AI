[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecopulse-ai"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "gymnasium",
    "torch"
]
